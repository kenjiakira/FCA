module.export.config = {
    name:"testing",
    description:"",
    usage: [],
    version: "1.0.0",
    credits:[]
}

module.exports.noprefix = (api, event, args, client) => {}

module.exports.run = (api, event, args, client) => {}

module.exports.onload = (api, client) => {}

module.exports.reply = (api, event, client) => {}

module.exports.reaction = (api, event, client) => {}
