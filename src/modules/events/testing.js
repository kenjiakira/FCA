module.export.config = {
    name:"testing",
    description:"",
    usage: [],
    version: "1.0.0",
    credits:[]
}


module.exports.run = (api, event, client) => {}

module.exports.onload = (api, client) => {}